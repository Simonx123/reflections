How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    Faster and neater. You can see where the error is directly without having to
    pace through out the entire code. This saves a whole lot of time

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    I started of with the beginner tutorial in android, after working on different
    applications, I noticed there are some changes I made that I will like to
    keep track of both the new and the old.
    Having access to history of my codes, we easily help solve this problem.
    To be able to move forward and backward while building an Application
    aid problem solving

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?

    Commit makes it easier to have control over when to save a version.
    This gives room to making commit only when there is a logic change in the
    code or application

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    Because in most cases, a project contains multiple files that are dependent

How can you use the commands git log and git diff to view the history of files?

  git log outputs the list of commit history for files in a repository
  git diff outputs the difference between two files

How might using version control make you more confident to make changes that
could break something?

    You can easily return back to the previuos file
Now that you have your workspace set up, what do you want to try using Git for?

    Version control for my android project

Version control for my android project
